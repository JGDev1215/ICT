# 06 - Manual QA Checklist

This file is retained as a compatibility pointer because older prompt packs referenced `06-manual-qa-checklist.md`.

Use the current release QA checklist instead:

```text
docs/ui-redesign/08-qa-release-checklist.md
```

Current status as of 2026-07-08:

- Automated smoke coverage exists in `tests/smoke.js`.
- Browser E2E coverage exists in `tests/e2e/planner.spec.js`.
- Real-device iOS Safari, Android Chrome, PWA install, and offline/service-worker verification still require manual device testing before a public release.
