(function(){
  const app = document.getElementById('app');
  const instruments = ['MNQ','NQ','MES','ES','MYM','YM','MGC','GC','CL','EURUSD','GBPUSD','BTCUSD'];
  const drawOptions = ['Previous day high (PDH)','Previous day low (PDL)','Previous week high (PWH)','Previous week low (PWL)','Previous month high (PMH)','Previous month low (PML)','Asia high','Asia low','London high','London low','New York AM high','New York AM low','Session high','Session low','Relative equal highs (REH)','Relative equal lows (REL)','Old high / prior swing high','Old low / prior swing low','Buy-side liquidity above price','Sell-side liquidity below price','New Week Opening Gap (NWOG)','New Day Opening Gap (NDOG)','HTF fair value gap / imbalance','Unfilled FVG / imbalance','Midnight open','Weekly open','Opening range high','Opening range low','Other mapped liquidity'];
  const times = ['London Killzone','Pre NY','NY AM','NY Lunch','London Close','During Macro','Outside Killzone','Today','Tomorrow','This week','Other / discretionary'];
  const confidence = ['Low','Medium','High'];
  const tfs = ['15m','5m','3m','1m'];
  let step = 0;
  let f = {date:'', instrument:'', session:'', sweepTime:'', sweepTf:'', sweepPool:'', sweepLevel:'', fvg:'', fvgTf:''};
  for(let i=1;i<=3;i++){ f['dol'+i+'Level']=''; f['dol'+i+'Draw']=''; f['dol'+i+'Confidence']=''; f['dol'+i+'HitTime']=''; }
  function e(s){return String(s||'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');}
  function opts(arr,val,ph){return '<option value="">'+e(ph)+'</option>'+arr.map(x=>'<option value="'+e(x)+'" '+(x===val?'selected':'')+'>'+e(x)+'</option>').join('');}
  function input(id,label,ph,type){return '<label class="label" for="'+id+'">'+label+'</label><input class="in '+(id.indexOf('Level')>-1?'numeric':'')+'" id="'+id+'" type="'+(type||'text')+'" value="'+e(f[id])+'" placeholder="'+e(ph||'')+'">';}
  function select(id,label,arr,ph){return '<label class="label" for="'+id+'">'+label+'</label><select class="in" id="'+id+'">'+opts(arr,f[id],ph||'— select —')+'</select>';}
  function line(k,v){return '<div class="line"><div class="k">'+k+'</div><div class="v">'+(v?e(v):'<span class="empty">—</span>')+'</div></div>';}
  function sync(){Object.keys(f).forEach(k=>{let n=document.getElementById(k);if(n)f[k]=n.type==='checkbox'?(n.checked?'yes':''):n.value.trim();});}
  function clean(){document.querySelectorAll('.numeric').forEach(n=>n.oninput=()=>{n.value=n.value.replace(/[^0-9.]/g,'').replace(/(\..*)\./g,'$1');});}
  function dol(i){return '<div class="panel"><div class="progress">DOL '+i+' of 3</div><div class="grid"><div>'+input('dol'+i+'Level','Draw on liquidity price level','price level')+'</div><div>'+select('dol'+i+'Draw','Draw rationale / liquidity draw',drawOptions,'— select liquidity draw —')+'</div></div><div class="grid"><div>'+select('dol'+i+'Confidence','Draw confidence',confidence,'— confidence —')+'</div><div>'+select('dol'+i+'HitTime','Hit time',times,'— expected hit time —')+'</div></div></div>';}
  function dolText(){let out=[];for(let i=1;i<=3;i++){let parts=[f['dol'+i+'Level']&&'Level '+f['dol'+i+'Level'],f['dol'+i+'Draw']&&'Liquidity draw: '+f['dol'+i+'Draw'],f['dol'+i+'Confidence']&&'Confidence: '+f['dol'+i+'Confidence'],f['dol'+i+'HitTime']&&'Hit time: '+f['dol'+i+'HitTime']].filter(Boolean);if(parts.length)out.push('DOL '+i+' — '+parts.join(' · '));}return out.join('  ');}
  function completed(){let c=0,p=0;for(let i=1;i<=3;i++){let any=f['dol'+i+'Level']||f['dol'+i+'Draw']||f['dol'+i+'Confidence']||f['dol'+i+'HitTime'];let ok=f['dol'+i+'Level']&&f['dol'+i+'Draw']&&f['dol'+i+'Confidence']&&f['dol'+i+'HitTime'];if(ok)c++;else if(any)p++;}return {complete:c,partial:p,valid:c>0&&p===0};}
  function summary(){return line('Instrument',f.instrument)+line('DOL stack',dolText())+line('Potential sweep',[f.sweepTime,f.sweepTf,f.sweepPool,f.sweepLevel].filter(Boolean).join(' · '))+line('FVG',f.fvg?('FVG formation · '+f.fvgTf):'Not confirmed');}
  function saveCard(){let cards=JSON.parse(localStorage.getItem('ict_cards_v073')||'[]');cards.unshift({savedAt:new Date().toISOString(),fields:{...f}});localStorage.setItem('ict_cards_v073',JSON.stringify(cards.slice(0,50)));step=4;render();}
  function saved(){let cards=JSON.parse(localStorage.getItem('ict_cards_v073')||'[]');if(!cards.length)return '<div class="panel"><p class="hint">No saved cards yet.</p></div>';let groups={};cards.forEach(c=>{let k=c.fields.instrument||'No instrument';(groups[k]||(groups[k]=[])).push(c);});return Object.keys(groups).sort().map(k=>'<div class="card"><h2>'+e(k)+'</h2>'+groups[k].map(c=>'<div class="saved-row"><small>'+e(new Date(c.savedAt).toLocaleString())+'</small><div class="focus-output">'+line('DOL stack',dolTextFor(c.fields))+line('Sweep',[c.fields.sweepTime,c.fields.sweepTf,c.fields.sweepPool,c.fields.sweepLevel].filter(Boolean).join(' · '))+'</div></div>').join('')+'</div>').join('');}
  function dolTextFor(x){let old=f;f=x;let t=dolText();f=old;return t;}
  function render(){let body='';if(step===0)body='<section class="screen"><div class="card"><div class="progress">Step 1 of 4</div><h2>Choose the instrument</h2><div class="grid"><div>'+input('date','Date','','date')+'</div><div><label class="label" for="instrument">Instrument</label><input class="in" id="instrument" list="inst" value="'+e(f.instrument)+'" placeholder="Select or type instrument"><datalist id="inst">'+instruments.map(x=>'<option value="'+x+'">').join('')+'</datalist></div></div>'+select('session','Session',['London','Pre-New York','New York AM','New York PM','Other'],'Optional')+'</div></section>';if(step===1){let v=completed();body='<section class="screen"><div class="card"><div class="progress">Step 2 of 4</div><h2>Define up to three draws on liquidity</h2><p class="sub">Draw rationale is now a dropdown of potential liquidity draws.</p>'+dol(1)+dol(2)+dol(3)+'<div class="focus-output">'+line('Completed DOLs',v.complete+' / 3')+line('Status',v.valid?'Ready':'Complete each started DOL row')+'</div></div></section>';}if(step===2)body='<section class="screen"><div class="card"><div class="progress">Step 3 of 4</div><h2>Potential sweep</h2>'+select('sweepTime','Potential sweep',times,'— select potential sweep —')+'<div class="grid"><div>'+select('sweepTf','Sweep timeframe',tfs,'— timeframe —')+'</div><div>'+select('sweepPool','Opposing pool of liquidity',drawOptions,'— select opposing liquidity —')+'</div></div>'+input('sweepLevel','Sweep level','price level')+'<div class="panel"><label class="check-row"><input type="checkbox" id="fvg" '+(f.fvg?'checked':'')+'> <span>FVG formation</span></label>'+select('fvgTf','FVG timeframe',tfs,'— FVG timeframe —')+'</div></div></section>';if(step===3)body='<section class="screen"><div class="card"><div class="progress">Step 4 of 4</div><h2>Review card</h2><div class="focus-output">'+summary()+'</div><div class="panel"><h3>Verification markers</h3><label class="check-row"><input type="checkbox"> <span>Verify draw on liquidity</span></label><label class="check-row"><input type="checkbox"> <span>Verify draw on liquidity for sweeps</span></label><label class="label">Outcome</label><select class="in"><option>Open</option><option>Hit</option><option>Miss</option><option>Breakeven</option><option>Read</option></select></div></div></section>';if(step===4)body='<section><div class="btn-row-between"><div><div class="progress">Saved cards</div><h2>Grouped by instrument</h2></div><button class="btn" id="newBtn">New</button></div>'+saved()+'</section>';app.innerHTML=body+(step<4?'<div class="bottom-nav"><div class="inner"><button class="btn" id="backBtn">Back</button><button class="btn primary" id="nextBtn">'+(step===3?'Save card':'Next')+'</button></div></div>':'');clean();let nx=document.getElementById('nextBtn'),bk=document.getElementById('backBtn'),nw=document.getElementById('newBtn');if(nx)nx.onclick=()=>{sync();if(step===3)saveCard();else{step++;render();}};if(bk)bk.onclick=()=>{sync();step=Math.max(0,step-1);render();};if(nw)nw.onclick=()=>{step=0;render();};}
  function tick(){let c=document.getElementById('nyClock');if(c)c.innerHTML=new Date().toLocaleTimeString('en-US',{timeZone:'America/New_York',hour12:false})+' <span>NY</span>';}
  setInterval(tick,1000);tick();render();
})();